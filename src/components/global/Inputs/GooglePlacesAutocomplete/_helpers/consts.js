import { signal } from '@preact/signals-react';

// Signal for the autocomplete instance
export const $autocomplete = signal(null);

// Signal for the selected place
export const $selectedPlace = signal(null);

